import { GroupTransformateur } from './group-transformateur';

describe('GroupTransformateur', () => {
  it('should create an instance', () => {
    expect(new GroupTransformateur()).toBeTruthy();
  });
});
