import { AddGroupTransformateur } from './add-group-transformateur';

describe('AddGroupTransformateur', () => {
  it('should create an instance', () => {
    expect(new AddGroupTransformateur()).toBeTruthy();
  });
});
